# Google Maps API key for Android native map tiles.
# Required for MapView on Android — without this key the Map tab shows a stable fallback.
# Set in EAS secrets or local .env before running `npx expo prebuild`.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
