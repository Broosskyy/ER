import { SettingsPlaceholderScreen } from '@/features/profile/components/SettingsPlaceholderScreen';

export default function SettingsAccountScreen() {
  return (
    <SettingsPlaceholderScreen
      title="Account"
      description="Account-Einstellungen werden in einem späteren Sprint angebunden."
    />
  );
}
