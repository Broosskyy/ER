# Next Steps — Sprint 5.7+

## Screenshot QA pipeline

1. Add `npm run capture:runtime` and `npm run capture:rendered` scripts to package.json.
2. Integrate Maestro flow for reliable guest → home → event detail navigation.
3. Capture onboarding slides 2–5 on Expo Web for full rendered_ui set.

## UI polish (Sprint 5.8)

4. Event Details mockup alignment (`11_event_detail.png`).
5. Fix onboarding duplicate button layout.
6. Filter picker modals (Datum/Genre/Ort) if approved as UX feature.

## QA

7. Physical device screenshot pass (no ANR).
8. Re-capture `11_event_detail.png` on device.
9. Full accessibility audit.

## Engineering

10. CI job: build APK → emulator screenshots → rendered UI → ZIP artifact upload.
